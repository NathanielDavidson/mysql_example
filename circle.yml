test:
  post:
    - mkdir -p $CIRCLE_TEST_REPORTS/junit/
    - cp ./*/*/*.xml $CIRCLE_TEST_REPORTS/junit/
    - cp ./*/*.war $CIRCLE_TEST_REPORTS/webapp
notify:
  webhooks:
    - url: http://csumbse.net/harbormaster/hook/circleci/